import { TExcel } from '../';
import { IExcelColumn, ICellProps } from '../../app';
/**
 * 渲染表格头
 * @param excel TExcel
 * @param columns 传入的表头配置，antd table columns props
 * @param style 表头样式
 * @param depth 表头深度
 * @param x 开始绘制的单元格，第几列
 * @param y 开始绘制的单元格，第几行
 */
export declare function renderThead(excel: TExcel, columns: IExcelColumn[], style: ICellProps['style'], depth: number, x?: number, y?: number): void;
