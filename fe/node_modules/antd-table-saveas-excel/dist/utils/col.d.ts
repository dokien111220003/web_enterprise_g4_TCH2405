import { Col } from 'better-xlsx';
import { IExcelColumn } from '../../app';
import { TExcel } from '../';
export declare function addCol(excel: TExcel, start: number, count: number, props: IExcelColumn[]): void;
declare function drawCol(col: Col, props: IExcelColumn): void;
export default drawCol;
