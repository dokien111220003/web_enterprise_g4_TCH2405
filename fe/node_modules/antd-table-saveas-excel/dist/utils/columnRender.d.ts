import { IStyle } from '../../app';
/**
 * 获取column render的结果的参数
 * @param data column render的结果
 */
declare function getColumnRenderValue(data: any): {
    children: string;
    colSpan: number;
    rowSpan: number;
    __style__: IStyle;
};
export default getColumnRenderValue;
