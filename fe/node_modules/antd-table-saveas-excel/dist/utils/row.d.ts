import { TExcel } from '../';
/**
 * 添加行
 * @param excel Excel实例
 * @param count 行数
 * @param height 行高(单位: CM)
 */
export declare function addRow(excel: TExcel, start: number, count: number, height: number): void;
